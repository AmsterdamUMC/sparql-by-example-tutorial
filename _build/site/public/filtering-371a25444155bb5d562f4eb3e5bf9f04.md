---
title: "Filtering Results"
---

# Filtering Results

*Content coming soon.*

## Learning objectives

- Use `FILTER` expressions with comparison operators
- Apply `REGEX` for string matching
- Filter by language tag with `LANG()`
- Use `BOUND()` and `isIRI()` tests
